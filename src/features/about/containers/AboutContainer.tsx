import { aboutPoints } from '@/data'
import { AboutSection } from '@/features/about/components'

const AboutContainer = (): React.JSX.Element => <AboutSection points={aboutPoints} />

export default AboutContainer
