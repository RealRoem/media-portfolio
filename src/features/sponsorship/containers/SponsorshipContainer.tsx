import { sponsorshipValues } from '@/data'
import { SponsorshipSection } from '@/features/sponsorship/components'

const SponsorshipContainer = (): React.JSX.Element => (
  <SponsorshipSection values={sponsorshipValues} />
)

export default SponsorshipContainer
