import { contactLinks } from '@/data'
import { ContactSection } from '@/features/contact/components'

const ContactContainer = (): React.JSX.Element => <ContactSection links={contactLinks} />

export default ContactContainer
