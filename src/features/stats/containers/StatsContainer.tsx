import { stats } from '@/data'
import { StatsSection } from '@/features/stats/components'

const StatsContainer = (): React.JSX.Element => <StatsSection stats={stats} />

export default StatsContainer
